<%= title [l(:label_title_ex_rels), { controller: 'external_relations', action: 'show'}], l(:label_relation_new) %>

<%= labelled_form_for :ex_rel, @ex_rel,
  url: { action: 'create' }, html: { multipart: true, id: 'ex_rel-form' } do |f| %>
  <%= render partial: 'form', locals: { form: f } %>
  <%= submit_tag l(:button_create) %>
<% end %>
